To executes application:

-Front End is builded with Angular 12.0.5. The application is in the path appExcelCar.
 You can copy this folder, and using NodeJs Console inside him, you can run the command ng serve -o.

-BackEnd is a .NET Core 5 Web API. To run the API, open the solution and run with Visual Studio.

